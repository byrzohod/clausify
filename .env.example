# ===========================================
# Clausify Environment Variables
# ===========================================

# ----- REQUIRED FOR PRODUCTION -----

# Database (Railway provides this automatically when you add PostgreSQL)
DATABASE_URL="postgresql://postgres:password@localhost:5432/clausify"

# NextAuth (REQUIRED - generate with: openssl rand -base64 32)
NEXTAUTH_URL="https://your-app.railway.app"
NEXTAUTH_SECRET="generate-a-secret-at-least-32-characters-long"

# AI Provider (REQUIRED for production)
AI_PROVIDER="anthropic"
ANTHROPIC_API_KEY="sk-ant-your-anthropic-api-key"

# ----- OPTIONAL -----

# Storage Provider: "local" for Railway, "supabase" if using Supabase Storage
STORAGE_PROVIDER="local"

# Stripe (for payments - optional until you enable billing)
STRIPE_SECRET_KEY="sk_test_your-stripe-secret-key"
STRIPE_WEBHOOK_SECRET="whsec_your-webhook-secret"
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_your-publishable-key"

# Supabase (only if using Supabase instead of Railway PostgreSQL)
# NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
# NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
# SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Sentry Error Tracking (optional)
# NEXT_PUBLIC_SENTRY_DSN="https://your-sentry-dsn@sentry.io/project-id"
# SENTRY_AUTH_TOKEN="your-sentry-auth-token"

# ----- LOCAL DEVELOPMENT ONLY -----

# For local dev with Ollama (don't set these in production)
# AI_PROVIDER="ollama"
# OLLAMA_URL="http://localhost:11434"
# OLLAMA_MODEL="llama3.2"
